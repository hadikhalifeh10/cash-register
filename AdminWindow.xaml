<Window x:Class="cashregister.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:cashregister.ViewModel"
        Title="Admin" Height="900" Width="1400"
        Background="#0B0B0D" Foreground="#F3F3F3">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#141418" Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Width="100" Click="Close_Click"/>
            </StackPanel>
        </Border>
        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Items with editor -->
            <GroupBox Header="Items (Admin)" Grid.Column="0" Margin="8" Padding="10" Background="#0C0C0F">
                <DockPanel>
                    <ListView ItemsSource="{Binding Items}" Name="ItemsEditor" DockPanel.Dock="Top">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="180">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Price" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Category" Width="140">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Add item UI (always visible in admin) -->
                    <StackPanel Orientation="Vertical" Margin="0,10,0,0" DockPanel.Dock="Bottom">
                        <TextBlock Text="Add new item" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Name="NewItemNameBox" Tag="NewItemName" Width="160" Height="28" Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBox Name="NewItemPriceBox" Tag="NewItemPriceText" Width="100" Height="28" Margin="6,0,0,0" Text="{Binding NewItemPriceText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBox Name="NewItemCategoryBox" Tag="NewItemCategory" Width="120" Height="28" Margin="6,0,0,0" Text="{Binding NewItemCategory, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <Button Content="Add Item" Width="120" Height="30" Margin="0,6,0,0" HorizontalAlignment="Left" Command="{Binding AddNewItemCommand}" />
                    </StackPanel>
                </DockPanel>
            </GroupBox>

            <!-- Right: Cart stays same as main for preview -->
            <GroupBox Header="Cart Preview" Grid.Column="1" Margin="8" Padding="10" Background="#0C0C0F">
                <TextBlock Text="Use main window for checkout. Admin focuses on item management."/>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>
